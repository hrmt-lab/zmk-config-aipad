#pragma once

/* The probe has no API: it is a self-contained thread compiled in only when
 * CONFIG_AIPAD_ENCODER_PROBE is set. This header exists so the translation unit
 * has something to include and is never empty. */
